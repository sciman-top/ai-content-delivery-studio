<UserControl x:Class="ContentDeliveryStudio.App.Views.QueueRowsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border BorderBrush="#D4DAE5" BorderThickness="1,0,1,1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding DataContext.ImageSeriesWorkspace.HasQueueRows, RelativeSource={RelativeSource AncestorType=TabControl}}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <ListBox ItemsSource="{Binding DataContext.ImageSeriesWorkspace.QueueRows, RelativeSource={RelativeSource AncestorType=TabControl}}"
                 SelectedItem="{Binding DataContext.ImageSeriesWorkspace.SelectedQueueRow, RelativeSource={RelativeSource AncestorType=TabControl}, Mode=TwoWay}"
                 AutomationProperties.AutomationId="QueueTaskList"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E3E7EF" BorderThickness="0,0,0,1" Padding="12,10">
                            <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.2*" />
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="1.4*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding ItemTitle}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Position}"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="10,0,0,0" />
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Status}"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="10,0,0,0" />
                                <TextBlock Grid.Column="3"
                                           Text="{Binding Attempts}"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="10,0,0,0" />
                                <TextBlock Grid.Column="4"
                                           Text="{Binding OutputPath}"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="#4B5563"
                                           Margin="10,0,0,0" />
                                <TextBlock Grid.Column="5"
                                           Text="{Binding ErrorMessage}"
                                           TextWrapping="Wrap"
                                           Foreground="#8A3B3B"
                                           Margin="10,0,0,0" />
                            </Grid>
                            <TextBlock Text="{Binding RequestSummary}"
                                       Foreground="#4B5563"
                                       FontSize="11"
                                       Margin="0,5,0,0"
                                       TextWrapping="Wrap" />
                            <TextBlock Text="{Binding ApprovalSummary}"
                                       AutomationProperties.Name="{Binding ApprovalSummary}"
                                       Foreground="#315B7D"
                                       FontSize="11"
                                       Margin="0,3,0,0"
                                       TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
    </Border>
</UserControl>

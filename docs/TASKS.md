# Remaining Tasks

There is no repo-executable implementation queue. The recorded V1 and scientific-figure implementation slices are closed; their historical evidence is not a command to rerun paid or manual work.

Only four external-authority blockers remain:

| Blocker | Required authority | Reopen condition | Repository boundary |
| --- | --- | --- | --- |
| Physics expert Gate 1 decision for the reconstructed six-figure article set | Named human domain expert | Exact candidate identity and review packet are available | Do not simulate or self-approve the expert decision. |
| One live scientific acceptance from the accepted Gate 1 identity | Explicit paid-provider authorization plus human Gate 2 review | Gate 1 is accepted and the user requests a fresh live run | Do not call a live provider automatically or replay an old request. |
| Packaged Windows accessibility and hardware acceptance | Named operator on the target Windows/hardware matrix | A release candidate is packaged for Narrator, high contrast, DPI, touch/pen, and device checks | Automated source or ViewModel tests are supporting evidence, not hardware acceptance. |
| Truthful new release evidence snapshot | Mixed release authority | Provider behavior, release candidate, or acceptance basis materially changes | Do not relabel the historical V1 or scientific snapshot as current without fresh matching evidence. |

Ordinary defects and maintenance discovered in code should be fixed directly with proportional tests. They do not require a queue entry, per-change plan, or evidence file.
